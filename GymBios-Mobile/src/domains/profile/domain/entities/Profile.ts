export interface Profile {
  id: string;
  userId?: string;
  name: string;
  email: string;
  phone?: string;
  address?: string;
  role: string;
  department?: string;
  branch?: string;
  staffId?: string;
  memberId?: string;
  joinDate?: string;
  photoUrl?: string;
  status?: string;
}
