import { CheckInHubScreen } from '@/domains/checkIn/presentation/screens';

/**
 * Check-In Hub route — the entry point of the Check-In module.
 */
export default function CheckInHubRoute() {
  return <CheckInHubScreen />;
}
