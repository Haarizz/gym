import { DailyVisitorsScreen } from '@/domains/checkIn/presentation/screens';

/**
 * Today's Daily Visitors route.
 * Lists all walk-in passes issued today.
 */
export default function DailyVisitorsRoute() {
  return <DailyVisitorsScreen />;
}
