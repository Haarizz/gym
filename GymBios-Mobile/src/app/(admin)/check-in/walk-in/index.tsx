import { WalkInHubScreen } from '@/domains/checkIn/presentation/screens';

/**
 * Walk-In / Daily Visitor Hub route.
 * Entry point for the walk-in sub-module: register visitors or view today's list.
 */
export default function WalkInHubRoute() {
  return <WalkInHubScreen />;
}
