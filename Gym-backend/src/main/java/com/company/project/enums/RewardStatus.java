package com.company.project.enums;

/** Lifecycle state of a single ReferralReward instance. */
public enum RewardStatus {
    PENDING,
    AVAILABLE,
    CLAIMED,
    REDEEMED,
    EXPIRED,
    CANCELLED
}
