package com.company.project.enums;

/** The UI action / redemption dispatch mechanism a reward exposes to the member. */
public enum RedemptionAction {
    AUTO_WALLET,
    USE_DURING_PAYMENT,
    EXTEND_MEMBERSHIP,
    BOOK_PT,
    BOOK_CLASS,
    COPY_COUPON,
    COLLECT_GIFT,
    REQUEST_CASH
}
